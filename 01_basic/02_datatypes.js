"use strict"; //treat all js code as newer version
// alert(3+3) using nodejs not browser
 
console.log(3
    +3 //code readability should be high
)

console.log(3+3);

let name="kanu"; //string
let age=20   //number
let islogged=false //boolean true/false

//null standalone value   let state=null (empty)
//undefined -> not assign value but declare variable 
// symbol -> used mostly in react, used for uniqueness

//object

console.log(typeof age);
console.log(typeof null) //output object